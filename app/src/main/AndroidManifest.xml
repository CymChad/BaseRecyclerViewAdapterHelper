<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chad.baserecyclerviewadapterhelper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/brvah_app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MyApplication"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" />
        <activity android:name=".ChooseMultipleItemUseTypeActivity" />
        <activity android:name=".AnimationUseActivity" />
        <activity android:name=".MultiItemQuickUseActivity" />
        <activity android:name=".MultiItemDelegateUseActivity" />
        <activity android:name=".MultiItemProviderUseActivity" />
        <activity android:name=".HeaderAndFooterUseActivity" />
        <activity android:name=".LoadMoreRefreshUseActivity" />
        <activity android:name=".SectionUseActivity" />
        <activity android:name=".EmptyViewUseActivity" />
        <activity android:name=".ItemClickActivity" />
        <activity android:name=".DataBindingUseActivity" />
        <activity android:name=".DragAndSwipeUseActivity" />
    </application>

</manifest>